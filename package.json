{
  "name": "hilan-bot",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "webhook": "node make-webhook.js",
    "build": "npm install && npx playwright install chromium",
    "postinstall": "npx playwright install chromium",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "playwright": "^1.8.0"
  },
  "dependencies": {
    "bluebird": "^3.7.2",
    "config": "^3.3.6",
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "js-yaml": "^4.1.0"
  }
}
